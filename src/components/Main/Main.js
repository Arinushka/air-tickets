import { Filters } from './../Filters/Filters';
import { Tickets } from './../Tickets/Tickets';

export const Main = () => {
	return (
		<main>
		<Filters></Filters>
		<Tickets></Tickets>
		</main>
	);
}
